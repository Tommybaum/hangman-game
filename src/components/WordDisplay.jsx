const WordDisplay = (props) => {
   // console.log(props.currentWord)
    return (
       
        <>
            <h1>WordDisplay</h1>
            <p>{props.currentWord}</p>

        </>
    )
}

export default WordDisplay